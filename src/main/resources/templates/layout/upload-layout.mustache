<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    {{#pageTitle}}<title>{{.}} - Cloud Image Gallery</title>{{/pageTitle}}
    {{^pageTitle}}<title>이미지 업로드 - Cloud Image Gallery</title>{{/pageTitle}}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .upload-container {
            min-height: calc(100vh - 56px); /* 56px는 네비게이션 바 높이 가정 */
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .upload-card { /* 이 스타일은 실제 콘텐츠를 담는 카드에 적용될 수 있도록 남겨둡니다. */
            width: 100%;
            max-width: 600px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        /* 미리보기 관련 스타일은 실제 업로드 폼이 있는 템플릿으로 이동하거나, */
        /* 이 레이아웃에 남겨둘 경우 해당 클래스를 사용하는 요소가 {{{body}}} 내에 있어야 합니다. */
        .upload-preview {
            width: 100%;
            height: 300px;
            border: 2px dashed #dee2e6;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
            overflow: hidden;
        }
        .upload-preview img {
            max-width: 100%;
            max-height: 100%;
            object-fit: contain;
        }
        .upload-preview-text {
            color: #6c757d;
            text-align: center;
        }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/">Cloud Image Gallery</a>
        <div class="navbar-nav">
            <a class="nav-link" href="/">갤러리로 돌아가기</a>
            {{#additionalNavLinks}} {{! 추가 네비게이션 링크가 필요할 경우를 위한 섹션 }}
                <a class="nav-link" href="{{url}}">{{text}}</a>
            {{/additionalNavLinks}}
        </div>
    </div>
</nav>

<div class="upload-container">
    {{{body}}} {{! 여기에 각 페이지의 실제 내용(업로드 폼 등)이 삽입됩니다. }}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{{#pageScripts}} {{! 페이지별 추가 스크립트를 위한 섹션 }}
    <script src="{{.}}"></script>
{{/pageScripts}}
</body>
</html>
